(function(angular) {
    angular.module('marineControllers').controller("SuggestionStatusController", ['$scope', '$http', '$routeParams',  '$filter',
        function($scope, $http, $routeParams, $filter) { 
            
        }]);
})(angular);